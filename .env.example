# Database — local dev
DATABASE_URL="postgresql://postgres@localhost:5432/your_database_name?schema=public"
# Database — Docker compose (overridden by docker-compose.yml, but fallback if needed)
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=xperimne

# NextAuth
AUTH_SECRET="generate with: openssl rand -base64 32"
AUTH_URL="http://localhost:3030"

# OAuth Providers (optional — kosongkan jika tidak dipakai)
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""
AUTH_GITHUB_ID=""
AUTH_GITHUB_SECRET=""

# App
NEXT_PUBLIC_APP_NAME="Xperimne"

# OpenRouter AI (daftar & buat API key di https://openrouter.ai/keys)
OPENROUTER_API_KEY="sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
